rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Leads: permitir escrita (site) e leitura (admin)
    match /leads/{leadId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
